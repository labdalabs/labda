# Model credential — pick ONE:
#   Vercel AI Gateway (matches the default model id in agent/agent.ts):
AI_GATEWAY_API_KEY=
#   ...or swap agent/agent.ts to @ai-sdk/anthropic and set:
# ANTHROPIC_API_KEY=sk-ant-...

# labda API the tools call.
LABDA_API_URL=http://localhost:3001/api/graphql
# A Supabase access token for the acting researcher.
LABDA_TOKEN=
