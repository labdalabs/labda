# Model credential — Vercel AI Gateway only (matches the model id in
# agent/agent.ts). Set this, or run `vercel link` for a VERCEL_OIDC_TOKEN.
AI_GATEWAY_API_KEY=

# labda API the tools call.
LABDA_API_URL=http://localhost:3001/api/graphql
# A Supabase access token for the acting researcher.
LABDA_TOKEN=
