{
  "name": "plugin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/plugin/src",
  "projectType": "application",
  "tags": ["scope:ui", "type:app", "target:client"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "apps/plugin"
      }
    }
  }
}
